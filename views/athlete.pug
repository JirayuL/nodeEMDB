extends layout

block content
  h1 #{athlete.name}
  h5 Trophy: #{athlete.trophy}
  p Description: #{athlete.description}
  hr
  if user
    a.btn.btn-light(href='/athletes/edit/'+athlete._id) Edit
    a.btn.btn-danger.delete-athlete(href='#', data-id=athlete.id) Delete
